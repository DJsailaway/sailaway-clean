import Image from "next/image";

// Boat card component
function BoatCard({ title, description, imageSrc }) {
  return (
    <div className="rounded-2xl overflow-hidden shadow-md bg-white hover:shadow-xl transition-shadow duration-300">
      <div className="relative w-full aspect-[16/9]">
        <Image
          src={imageSrc}
          alt={title}
          fill
          className="object-cover"
        />
      </div>
      <div className="p-4">
        <h3 className="text-xl font-semibold mb-2">{title}</h3>
        <p className="text-gray-600 text-sm leading-relaxed">
          {description}
        </p>
      </div>
    </div>
  );
}

// Example page usage
export default function BoatsPage() {
  const boats = [
    {
      title: "Plymouth Pilot",
      description:
        "A classic, sturdy pilot boat designed for coastal cruising and reliability in all conditions.",
      imageSrc: "/images/plymouth-pilot.jpg",
    },
    {
      title: "Bass Boat",
      description:
        "A lightweight, fast fishing boat built for agility, precision, and freshwater adventures.",
      imageSrc: "/images/bass-boat.jpg",
    },
  ];

  return (
    <div className="max-w-5xl mx-auto p-6">
      <h1 className="text-3xl font-bold mb-6">Our Boats</h1>

      <div className="grid grid-cols-1 md:grid-cols-2 gap-6">
        {boats.map((boat) => (
          <BoatCard
            key={boat.title}
            title={boat.title}
            description={boat.description}
            imageSrc={boat.imageSrc}
          />
        ))}
      </div>
    </div>
  );
}
